<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.spring.test.dao.boardMapper">

	<!-- 회원별 글 조회 -->
	<select id="listByUser" parameterType="String" resultType="Board">
		select
			boardnum as boardNum
			,title as boardTitle
			,to_char(inputdate, 'yyyy-mm-dd') as inputDate
			,id as memberId
		from
			blog_board
		where
			id = #{memberId}
	</select>

	<!-- 글 쓰기 -->
	<insert id="writeBoard" parameterType="Board">
		insert into
    		blog_board(
    			boardnum
    			,title
    			,content
    			,id
    		)values(
	    		blog_board_seq.nextval
	    		,#{boardTitle }
	    		,#{boardContent }
	    		,#{memberId }
			)
	</insert>
	
	<!-- 글 읽기 -->
	<select id="readBoard" parameterType="int" resultType="Board">
		select
			boardnum as boardNum
			,title as boardTitle
			,inputdate as inputDate
			,content as boardContent
			,likecnt as likeCnt
			,id as memberId
		from
			blog_board
		where
			boardnum = #{boardNum}
	</select>
	
	<!-- 글 삭제 -->
	<delete id="deleteBoard" parameterType="int">
		delete from
			blog_board
		where
			boardnum = #{boardNum}
	</delete>
	
	<!-- 글 수정 -->
	<update id="updateBoard" parameterType="Board">
		update
	  		blog_board
	  	set
	  		title = #{boardTitle }
	  		,content = #{boardContent }
	  	where
	  		boardnum = #{boardNum }	
	</update>
	
	<!-- 추천수 증가 -->
	<update id="updateLikeCnt" parameterType="int">
		update
	  		blog_board
	  	set
	  		likecnt = likecnt + 1
	  	where
	  		boardnum = #{boardNum }
	</update>
	
</mapper>